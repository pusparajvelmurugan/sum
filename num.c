#include<stdio.h>
int main()
{
char str[100];
int countnumeric;
int counter;
countDigits=countnumeric;
printf("Enter a string: ");
scanf("%c",str);
for(counter=0;numeric[counter]!=NULL;counter++)
{
if( countnumeric++)
}
printf("\nDigits: %d \ncountnumeric: %d",countnumeric);
return 0;
